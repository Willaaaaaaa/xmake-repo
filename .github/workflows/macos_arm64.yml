name: macOS (arm64)

on:
  pull_request:
    branches:
      - dev

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-14]
        arch: [arm64]
        kind: [static, shared]

    runs-on: ${{ matrix.os }}

    concurrency:
        group: ${{ github.ref }}-${{ github.base_ref }}-${{ github.head_ref }}-macOS-${{ matrix.arch }}-${{ matrix.kind }}
        cancel-in-progress: true
    steps:
      - uses: actions/checkout@v1
      - uses: xmake-io/github-action-setup-xmake@v1
        with:
          xmake-version: branch@master
          actions-cache-folder: '.xmake-cache'

      - name: Tests
        run: |
          wget https://curl.haxx.se/ca/cacert.pem -O /tmp/cacert.pem
          export CURL_CA_BUNDLE=/tmp/cacert.pem
          sudo ln /opt/homebrew/bin/gfortran-14 /opt/homebrew/bin/gfortran
          sudo mkdir /opt/homebrew/gfortran
          sudo ln -s /opt/homebrew/Cellar/gcc@14/14.3.0/lib/gcc/14 /opt/homebrew/gfortran/lib
          export PATH="$PATH:/opt/homebrew/Cellar/gcc@14/14.3.0/libexec/gcc/aarch64-apple-darwin23/14/"
          export LIBRARY_PATH="$LIBRARY_PATH:/opt/homebrew/Cellar/gcc@14/14.3.0/lib/gcc/14/"
          export LD_LIBRARY_PATH="$LD_LIBRARY_PATH:/opt/homebrew/Cellar/gcc@14/14.3.0/lib/gcc/14/"
          
          echo "------------------------------------------------------------------------------"
          echo "------------------------------------------------------------------------------"
          echo "------------------------------------------------------------------------------"
          echo "=== 验证gfortran路径 ==="
          ls -la /opt/homebrew/bin/gfortran-14 || echo "!!! /opt/homebrew/bin/gfortran-14 不存在"
          ls -la /opt/homebrew/bin/gfortran || echo "!!! /opt/homebrew/bin/gfortran 链接不存在"
          
          echo "=== 验证GCC路径 ==="
          ls -la /opt/homebrew/Cellar/gcc@14/14.3.0/lib/gcc/14 || echo "!!! /opt/homebrew/Cellar/gcc@14/14.3.0/lib/gcc/14 不存在"
          ls -la /opt/homebrew/Cellar/gcc@14/14.3.0/libexec/gcc/aarch64-apple-darwin23/14/ || echo "!!! /opt/homebrew/Cellar/gcc@14/14.3.0/libexec/gcc/aarch64-apple-darwin23/14/ 不存在"
          ls -la /opt/homebrew/gfortran/lib || echo "!!! /opt/homebrew/gfortran/lib 链接不存在"
          
          echo "=== 验证环境变量 ==="
          echo "PATH: $PATH"
          echo "LIBRARY_PATH: $LIBRARY_PATH" 
          echo "LD_LIBRARY_PATH: $LD_LIBRARY_PATH"
          
          echo "=== 测试gfortran命令 ==="
          which gfortran || echo "!!! which gfortran 未找到"
          gfortran --version || echo "!!! gfortran --version 无法执行"
          echo "------------------------------------------------------------------------------"
          echo "------------------------------------------------------------------------------"
          echo "------------------------------------------------------------------------------"
          
          xmake l ./scripts/test.lua -D -a ${{ matrix.arch }} -k ${{ matrix.kind }}
